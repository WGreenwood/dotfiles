[module/date]
type = internal/date
interval = 1.0

time = %a, %b %d, %I:%M:%S %p

format = <label>

label = %time%

format-prefix = %{T4} %{T-}
format-prefix-foreground = ${theme.icons}
format-padding = 2